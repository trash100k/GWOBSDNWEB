import Content from '../ui/Content.jsx'

// The home journey — the full scroll-jacked forge experience, unchanged.
// (Its own 3D wonderland is the obsidian slab already mounted in the shell.)
export default function Home() {
  return <Content />
}
